{
  "name": "@agnicid/tooling",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "echo \"No build step for tooling\"",
    "clean": "rimraf dist",
    "lint": "eslint scripts --ext .ts",
    "keygen": "tsx ./scripts/keygen.ts"
  },
  "dependencies": {
    "@agnicid/agent-sdk": "file:../packages/agent-sdk"
  },
  "devDependencies": {
    "rimraf": "^5.0.5"
  }
}
